import React from 'react'
import DrinksCard from './DrinksCard'

const DrinkCards = ({text}) => {
    return (
        <div>
            <DrinksCard text={text}/>
        </div>
    )
}

export default DrinkCards